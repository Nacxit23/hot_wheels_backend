type Rol {
    id: ID! @globalId
    name: String!
    users: [User!]! @hasMany(type: "connection")
}

extend type Query {
    rols: [Rol!]! @field(resolver:"App\\GraphQL\\Queries\\Rol\\rolQuery@__invoke")
}
